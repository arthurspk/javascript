// Exercicio de Número e Operadores

// Qual o resultado da seguinte expressão

var total = 10 + 5 * 2 / 2 + 20;
var resultado = 35;

// Cria duas expressões que retornem NaN

var expressao1 = '2kg';
var expressao2 = 3;
var resultado1 = (expressao1 / expressao2);
console.log(resultado1)

var numero1 = 'Dez'
var numero2 = 2;
var resultadoDaSoma = (numero1 * numero2);
console.log(resultadoDaSoma)

// Somar a string '200' com o número 50 e retornar 250

var valor1 = '200';
var valor2 = 50;
var resultadoDosValores = (+valor1 + valor2)
console.log(resultadoDosValores)

// Incremente o número 5 e retorne o seu valor incrementado

var incremento = 5;
var incrementado = (++incremento)
console.log(incrementado)

// Como dividir o peso por 2?

var numero = +'80' / 2;
var unidade = 'kg';
var peso = numero + unidade; // 80kg
console.log(pesoPorDois)
